#!/bin/bash

echo "=== Setting up Phase 6: Multi-Agent Features ==="

if [ ! -d "venv" ]; then
    python3 -m venv venv
fi

source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

streamlit run app.py --server.port 8502
